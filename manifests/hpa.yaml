apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: main
  labels:
    app.kubernetes.io/name: main
    app.kubernetes.io/part-of: main-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: main
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80