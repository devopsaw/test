apiVersion: v1
kind: Service
metadata:
  name: main
  namespace: default
  labels:
    app.kubernetes.io/name: main
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: main
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80